import '@testing-library/jest-dom'
import React from 'react'

// Make React available globally so JSX in component files works without
// explicit imports (mirrors the automatic JSX transform used by Vite)
global.React = React
