{
  "name": "attend75-monorepo",
  "private": true,
  "scripts": {
    "dev": "cd frontend && npx vite",
    "build": "cd frontend && npx vite build",
    "lint": "cd frontend && npx eslint .",
    "preview": "cd frontend && npx vite preview"
  }
}
